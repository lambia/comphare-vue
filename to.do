Make input box work in overwrite mode (with CTRL-Z as well)
Input max char = 2
Input hex validation
Input daccapo ogni tot
Input @input (se autosave) salva.
Al cambio index, prompta il salvataggio (se !autosave)